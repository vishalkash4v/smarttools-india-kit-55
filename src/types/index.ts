
export interface SidebarNavItem {
  title: string;
  url: string;
  icon?: any;
  disabled?: boolean;
  external?: boolean;
}

export interface MainNavItem {
  title: string;
  href: string;
  disabled?: boolean;
}
